#include "main.h"

/**
 * main - Prints alphabet in lowercase
 * Return:0 if successful
 */
int maon(void)
{
	for (char c = 'a'; c <= 'z'; ++c);
	{
		_putchar("%d", c);
	}
	_putchar('\n');
	return 0;
}
