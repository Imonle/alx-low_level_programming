#include <stdio.h>

/**
 * main - Prints single digit numbers of base 10
 * Return: Always 0
 */
int main(void)
{
	int (0,1,2,3,4,5,6,7,9);
	char b;

	for (b = 0; b <= 9; b++)
		printf((b)('\n'));
	return (0);
}
